server:
  port: 8081

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/yuelu_tourism?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 123456

mybatis-plus:
  configuration:
    # 控制台打印 SQL，便于新手调试（后续上线可关闭）
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

# JWT 配置（登录成功后签发 Token 用）
jwt:
  secret: yuelu-tourism-jwt-secret-key-32bytes!!
  expire-hours: 72

# AI 智能导游大模型配置 (火山引擎 Bot 应用版)
ai:
  mock-enabled: false
  base-url: https://ark.cn-beijing.volces.com/api/v3/bots/chat/completions
  api-key: ark-758ace1a-d30e-496e-af8d-8e95698e61df-2fd43
  model: bot-20260421164545-xl69h